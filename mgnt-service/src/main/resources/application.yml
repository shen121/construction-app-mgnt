spring:
  profiles:
    active: dev

mybatis:
  mapper-locations: classpath:mybatis-mapper/*/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      demo:
        mgnt:
          dao: debug
